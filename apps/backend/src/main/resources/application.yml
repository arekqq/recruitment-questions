spring:
  application:
    name: RecruitmentQuestions
  datasource:
    url: jdbc:postgresql://localhost:5432/rq
    username: root
    password: admin_password
  jpa:
    hibernate:
      ddl-auto: update
  properties:
    hibernate:
      dialect: org.hibernate.dialect.PostgreSQLDialect